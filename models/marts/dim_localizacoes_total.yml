
version: 2

models:
  - name: dim_localizacoes_total
    description: Essa é a dimensão com dados de cidades.
    columns:
      - name: sk_localizacao
        description: Essa é a chave surrogate key da dimesao cidades.
        tests:
          - unique
          - not_null