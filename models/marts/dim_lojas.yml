
version: 2

models:
  - name: dim_lojas
    description: Essa é a dimensão com dados de lojas.
    columns:
      - name: sk_loja
        description: Essa é a chave surrogate key da dimesao lojas.
        tests:
          - unique
          - not_null