
version: 2

models:
  - name: fct_vendas
    description: Essa é a dimensão com dados de vendedores.
    columns:
      - name: sk_venda
        description: Essa é a chave foreign key da fatos vendas.
        tests:
          - unique
          - not_null