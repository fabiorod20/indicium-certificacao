
version: 2

models:
  - name: dim_vendedores
    description: Essa é a dimensão com dados de vendedores.
    columns:
      - name: sk_vendedor
        description: Essa é a chave surrogate key da dimesao vendedores.
        tests:
          - unique
          - not_null